# Ensure package-lock.json is always created and updated
package-lock=true

# Respect semver ranges in package.json (don't pin exact versions)
save-exact=false

# Enforce Node and npm version requirements from package.json
engine-strict=true

